/* Background Gradient */
body {
  margin: 0;
  font-family: Arial, sans-serif;
  background: linear-gradient(135deg, #74ebd5, #4d5577);
  color: #05226f;
}

/* Navbar */
header {
  background: rgba(79, 13, 80, 0.7);
  color: white;
  padding: 15px 30px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

header h1 {
  margin: 0;
  font-size: 24px;
}

nav a {
  color: white;
  text-decoration: none;
  margin-left: 20px;
  font-weight: bold;
}

nav a:hover {
  text-decoration: underline;
}

/* Container */
.container {
  padding: 40px;
  text-align: center;
}

.container h2 {
  margin-bottom: 20px;
  font-size: 28px;
  color: #222;
}

/* Product Grid */
.product-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: 20px;
  margin-top: 20px;
}

/* Product Card */
.card {
  background: white;
  border-radius: 12px;
  box-shadow: 0 6px 15px rgba(0,0,0,0.15);
  padding: 20px;
  transition: transform 0.2s;
}

.card img {
  max-width: 100%;
  border-radius: 8px;
}

.card h3 {
  margin: 15px 0 10px;
}

.card p {
  color: green;
  font-weight: bold;
}

.card button {
  background: #e7fa1c;
  border: none;
  padding: 10px 20px;
  cursor: pointer;
  color: black;
  font-weight: bold;
  border-radius: 5px;
  margin-top: 10px;
}

.card button:hover {
  background: #e68a00;
}

.card:hover {
  transform: translateY(-5px);
}
